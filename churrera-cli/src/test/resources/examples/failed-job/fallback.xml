<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.3.0/pml.xsd">

    <title>Fallback Prompt</title>

    <goal>
        Stop all activities activities from the Agent.
        So, if the Agent is running a bash script, stop it or a Java maven build, stop it.

        If you have any local changes, commit them and push to the branch.

        if a running process has some concerns, share a summary.

        Put the Agent in a terminal state as FINISHED.
    </goal>
</prompt>
